<% include header %>
<p>
    <% if(user && user.username == doc.name ){%>
    <span>
        <a class="edit" href="/edit/<%= doc.name %>/<%= doc.title %>/<%= doc.time %>">编辑</a>
    </span>
    <span>
        <a class="edit" href="/remove/<%= doc.name %>/<%= doc.title %>/<%= doc.time %>">删除</a>
    </span>
    <% } %>
</p>
<p class="info">
    作者:<a href="/u/<%= doc.name %>"><%= doc.name %></a> |
    日期:<%= doc.time %>
</p>
<p>
    <%- doc.content %>
</p>
<% include comment %>
<% include footer %>